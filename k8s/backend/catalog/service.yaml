apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: retail-app
  labels:
    app: catalog
spec:
  selector:
    app: catalog
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
