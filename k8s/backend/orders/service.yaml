apiVersion: v1
kind: Service
metadata:
  name: orders-api-service
  namespace: retail-app
  labels:
    app: orders-api
spec:
  selector:
    app: orders-api
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
