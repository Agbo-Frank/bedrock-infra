# DO NOT commit real values to git.
# After terraform apply, populate this secret with the actual password:
#
# PASSWORD=$(aws secretsmanager get-secret-value \
#   --secret-id project-bedrock/rds/postgres \
#   --query SecretString --output text | jq -r .password)
#
# kubectl create secret generic orders-secret \
#   --namespace retail-app \
#   --from-literal=RETAIL_ORDERS_PERSISTENCE_PASSWORD=$PASSWORD \
#   --dry-run=client -o yaml | kubectl apply -f -

apiVersion: v1
kind: Secret
metadata:
  name: orders-secret
  namespace: retail-app
  labels:
    app: orders
type: Opaque
stringData:
  RETAIL_ORDERS_PERSISTENCE_PASSWORD: "PLACEHOLDER"
