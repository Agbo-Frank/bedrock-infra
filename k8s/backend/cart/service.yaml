apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: retail-app
  labels:
    app: cart
spec:
  selector:
    app: cart
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
