apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail-app
  labels:
    app: checkout
spec:
  selector:
    app: checkout
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
